/*
 * CuWatch WiFi and MQTT credentials.
 * Copy this file to my_secrets.h and fill in your credentials.
 * my_secrets.h is gitignored and must never be committed.
 */
#pragma once

/* WiFi credentials */
#define WIFI_SSID     "RedRover"
#define WIFI_PASSWORD NULL      /* NULL for open (WPA2-Enterprise) networks */

/* MQTT broker */
#define MQTT_BROKER   "pepper.physics.cornell.edu"
#define MQTT_PORT     1883

/* NTP server */
#define NTP_SERVER    "ntp3.cornell.edu"
