CompileFlags:
  # Use the compile_commands.json generated by CMake so clangd gets the
  # correct cross-compilation flags, include paths, and defines for the
  # RP2040 target (pico-sdk, FatFS, Mongoose, FreeRTOS headers, etc.).
  CompilationDatabase: build
